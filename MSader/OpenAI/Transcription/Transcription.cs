using Microsoft.Extensions.Options;
using OpenAI.Audio;

namespace MSader.OpenAI.Transcription
{
    public class Transcription
    {
        public void GetTextFromSpeech(Stream stream)
        {
            //AudioClient client = new("whisper-1", Environment.GetEnvironmentVariable("OPENAI_API_KEY"));

            //string audioFilePath = Path.Combine("Assets", "audio_houseplant_care.mp3");

            //AudioTranscription transcription = client.TranscribeAudioAsync(stream, audioFilePath);

            //return TranscribeAudio(audioStream, audioFilePath, options);

            //Console.WriteLine($"{transcription.Text}");
        }

    }

}
